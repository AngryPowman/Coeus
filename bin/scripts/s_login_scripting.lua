-- This script is use to handle login of players

function on_login(fist_login)
	if first_login == true then
		epic_type = epicType();
		
		if (epic_type == 0) then
		elseif epic_type == 1 then
		elseif epic_type == 2 then
		end
		accept_mission(10000);
	end
end